#include<stdio.h>
int main ()
{
  int num1,num2,sum;
  printf("Enter two integer number :");
  scanf("%d %d",&num1,&num2);
  sum = num1 + num2;
  printf("\nYour sum of two number is %d + %d = %d\n",num1,num2,sum);
  return 0;
}
